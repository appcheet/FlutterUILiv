import 'package:flutter/material.dart';

class AppConstants {
  static const String appTitle = 'Flutter UI Examples';
  static const String newBadgeText = 'NEW';
  
  static const double defaultPadding = 16.0;
  static const double smallPadding = 8.0;
  static const double largePadding = 20.0;
  
  static const double defaultBorderRadius = 20.0;
  static const double smallBorderRadius = 12.0;
  static const double containerBorderRadius = 15.0;
  
  static const double cardElevation = 0.0;
  static const double shadowBlurRadius = 10.0;
  static const double shadowOffset = 5.0;
  
  static const double iconSize = 30.0;
  static const double smallIconSize = 20.0;
  static const double arrowIconSize = 20.0;
}

class AppColors {
  static const primaryBlue = Color(0xFF2196F3);
  static const primaryPurple = Color(0xFF9C27B0);
  static const primaryGreen = Color(0xFF4CAF50);
  static const primaryOrange = Color(0xFFFF9800);
  static const primaryRed = Color(0xFFE91E63);
  static const primaryCyan = Color(0xFF00BCD4);
  static const primaryGrey = Color(0xFF9E9E9E);
  static const primaryPink = Color(0xFFE91E63);
  static const primaryDeepPurple = Color(0xFF673AB7);
  static const primaryTeal = Color(0xFF009688);
  static const primaryIndigo = Color(0xFF3F51B5);
  static const primaryLightBlue = Color(0xFF03A9F4);
  static const primaryAmber = Color(0xFFFFC107);
  
  static const newBadgeColor = Color(0xFFFF9800);
  static const shadowColor = Color(0x1A000000);
  static const overlayColor = Color(0x1AFFFFFF);
}